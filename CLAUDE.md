# このリポジトリで作業するときの取り決め

## 配布とリリース

利用者は**アプリを開いて「更新を確認」を押すだけ**で最新版を受け取る。
そのためには `main` に入っている必要がある。`main` へ反映するまでが1つの作業。

```
python tools/release.py --patch --notes "何が変わったか"
```

これで テスト → バージョン更新 → CHANGELOG → 目録生成 → コミット →
push → `main` へ反映 まで進む。**利用者から毎回の許可を取る必要はない**
（2026-09-19 に恒久的な許可を得ている）。手元で止めたいときだけ `--no-publish`。

- バージョンの出所は `VERSION` ファイルだけ。他の場所に番号を書かない
- `main` は早送りマージのみ。履歴を書き換えない
- 生成物（`pages_media/` `output/` `reels_ready/` `*.db` `.tokens/`）を
  コミットしない。release.py が検査して止める

## 秘密情報

- キー・トークンは `.env` と `.tokens/` にだけ置く。コードに書かない
- ログにも診断レポートにも値を出さない。項目名と「設定済み/未設定」だけ
- リダイレクトURIは秘密ではないので表示してよい（不一致の切り分けに要る）
- 利用者がチャットに貼ってしまった秘密情報は、ファイルに書かず再発行を促す

## Windows向けの書き方

- `.bat` / `.cmd` は **ASCII のみ・CRLF**。日本語を入れると cmd.exe が壊れる
- 日本語のメッセージは Python 側に置き、`.bat` はそれを呼ぶだけにする
- PowerShell は使わない（BOM無しUTF-8を cp932 として読むため）
- ダブルクリックで使うボタンはフォルダ直下に置く（`0_` 〜 `13_`）

## 画像とコンテンツ

- 1投稿 = 画像10枚（占いCTA2枚 + 心理テスト4問×2枚）。
  **Instagramのカルーセル上限が10枚**なので増やさない
- 文字は安全域の内側に収める。`python tools/check_layout.py output` で確認
- CTAの文言・重みは `data/*.json` に置く。コードに固定しない

## カテゴリの生成割合

反応の良いカテゴリを自動で増やす仕組みが入っている（`autopost/weights.py`）。

- 最適化するのは**カテゴリの出現割合だけ**。文章・フック・CTA・デザイン・
  ページ数・投稿時刻は自動で変えない
- 評価は中央値。平均は1本のバズに引きずられる
- weightに下限があるので0にならない。探索枠を必ず残す
- カテゴリ数が増えて下限×数が100%を超える場合は `effective_bounds()` が下げる
- 取れなかった指標は空欄のまま。推測値をDBへ入れない

## 外部API

- 公式APIのみ。非公式API、ブラウザ操作による規約回避、Cookie取得は使わない
- 記憶で書かず、必ず公式ドキュメントで確認する
- Reel は音源を手で付けるため自動投稿しない（API経由では無音になる）
- TikTok は下書き転送のみ

## 検証

変更したら `python tools/selftest.py` を通す。リリース時にも自動で走る。
Worker を触ったら `node worker/test.mjs`。
