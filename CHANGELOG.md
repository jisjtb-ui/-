# 変更履歴

書き方: 新しいバージョンを一番上に足す。`## v1.2.0` の下に箇条書きで
「使う人から見て何が変わったか」を書く。ここに書いた内容が
アプリの更新画面にそのまま表示される。

## v1.17.1

- 生成完了の表示が「画像10枚」で固定されていたのを、実際の構成から計算するように修正（11枚でも10枚と出ていた）

## v1.17.0

- 1枚目フックのA/B/Cテストを追加。1枚目フック + 5問×2 = 11枚の構成にできる
- フックの文言は data/hooks.json で管理。D・E… は1件足すだけで増やせる
- 自動ローテーションは回数だけでなく時間帯の位置も偏らないようにずらす
- hook_variant を meta.json → DB → 反応データまで通し、フック別に比較できるようにした
- 比較は保存率・共有率・コメント率（各数÷表示数）の中央値。総再生数だけで決めない
- 各フック10件そろうまでは「まだ判断しない」と表示する
- Instagramは10枚が上限のため、11枚構成のときは生成時に警告する（4問=9枚なら全媒体可）

## v1.16.0

- クラウド予約投稿を追加。登録後はPCの電源を切っても指定時刻に投稿される（Cloudflare Workers + D1 + Cron Triggers）
- 16_クラウドへ渡す.bat / 17_クラウドの状況.bat を追加
- スマホ用の管理画面をWorker自身が返すようにした。次の投稿・件数・失敗の確認と、いますぐ投稿/時刻変更/取消/再実行ができる
- 投稿先ごとの違いをAdapterに分けた（Instagram / Threads / TikTok）。YouTube Shorts や X を足しやすくしてある
- トークンをD1へ暗号化して保存し、期限前にクラウド側で自動更新する（Threads/Instagram 60日、TikTok 24時間）
- 失敗の種類を分けて再試行する。権限不足・トークン無効・規約違反は再試行せず、投稿内容は消さない
- ntfyでスマホへ通知する（特に失敗時）
- 反応データの取得もクラウド側で行い、PCへ取り込めるようにした

## v1.15.1

- Instagramの反応データで、使えない指標が1つ混ざると応答全体が落ちて視聴回数が入らなかったのを修正（断られたときだけ1つずつ聞き直す）
- 15_投稿を調べる.bat を追加。公開済み投稿の種類（Reelかカルーセルか）とAPIが実際に返す値を確認できる

## v1.15.0

- Category / SubCategory を作って選ぶ仕組みを追加。名前やIDを打ち直す場面をなくした
- 画面を4タブ構成にした（運用 / Category / 生成 / 成績）。14_画面をひらく.bat で開く
- SNS接続をCategoryから開始し、認証後に接続先を聞き直さない。既存の接続はそのまま引き継ぐ
- 投稿先をCategoryに紐づくアカウントから自動で決める
- weightをSubCategory単位にし、媒体内の相対値で比較するようにした（媒体の規模差で偏らない）
- 同じ測定データでweightが二重に動かないようにした
- 遅れて測った反応データを遅延として記録し、通常の評価に混ぜないようにした。計測区分は設定可能（7dを追加）
- TikTokの反応データ取得を追加。下書き転送と写真投稿は取得不可として記録する
- 反応データの取得結果を測定済み/取得失敗/取得不可/レート制限で分けて数えるようにした
- engine側でreachと視聴時間が保存されていなかったのを修正。保存経路を1本化した

## v1.14.1

- 配布の目録に新規ファイルが載らない不具合を修正（weights.py などが届かず更新後に動かなくなっていた）
- weightが読めないときの均等抽選が失敗していたのを修正
- 自動補充がカテゴリ指定なしで生成していたため、weightが次回生成に反映されていなかったのを修正
- Instagramの反応データ取得で、権限不足・使えない指標・レート制限・通信失敗を区別。空の結果を測定済みとして保存しない

## v1.14.0

- 反応の良いカテゴリの生成割合を自動で増やす
- 13_成績を見る.bat と GUIの「成績を見る」を追加。weightが変わった理由が読める
- Instagramの reach を独立した指標として保存（impressionsへ混ぜない）

## v1.13.0

- 答えの作り方を根本から変更。選択肢の意味を説明するのをやめ、選択肢から導けないことを言い当てる形にした
- 独占欲・一人の時間・秘密・本音・マチアプ・スキンシップの6カテゴリ216個を新基準で作り直し

## v1.12.2

- 答えの書き直しを継続。マチアプ・ホテル・雰囲気・スキンシップ・欲求・主導権の6カテゴリ216個

## v1.12.1

- 手で投稿するとき順番が崩れる問題を修正。画像の更新日時を1分ずつずらし、名前順でも日時順でも同じ並びになるようにした

## v1.12.0

- 1枚目にコメント誘導を追加。「一番当たってたの何番？」を投稿ごとに文を変えて小さく入れる
- 12通りを順番に配り、使い切るまで同じ文を出さないようにした
- 問題数は文中に自動で反映される（4問なら「4問のうち」）

## v1.11.0

- 認証の受け取り口ページ（/callback/）を追加。コードをコピーできるので手動認証が楽になる
- 認証に失敗した場合は、その理由もページに表示する

## v1.10.2

- 占いの2枚が投稿に含まれていなかったのを修正。画像の絞り込みが question/answer だけを見ていた

## v1.10.1

- 作り直したときに、使われなくなった古い画像を消すようにした（公開先に残り続けていた）
- セットアップの最後にスマホ用ページを作り直すようにした（古い一覧のままだった）

## v1.10.0

- セットアップで「作り直す / 足す」を選べるようにした。仕様が変わった後の古い投稿を残さないため
- experiment discard を追加。配信済みの記録は消さない（二重投稿の判断に必要なため）

## v1.9.2

- 公開後、.env の公開URLがダミーや実在しない値なら自動で直すようにした（スマホ用ページが開けない原因）

## v1.9.1

- 答えの踏み込みを強くした。独占欲・一人の時間・秘密・本音の4カテゴリ144個を書き直し
- 選択肢の言い換えをやめ、隠している動機や後から効いてくることを指すようにした

## v1.9.0

- 占いの選択肢からプロフィールを外し、保存を入れた（4つとも価値のある行動にするため）
- 1枚目に「心理テストは恋人とやってみて🌸」を表示するようにした
- 答えが選択肢の言い換えにならないよう、独占欲カテゴリの40個を書き直した

## v1.8.0

- 占いCTAを「選ぶ」「答え」の2枚組に変更。結果を先に見せないことで、押す理由とめくる理由を作った
- 差し込み位置を設定で変えられるようにした（既定は1・2枚目）
- 占いで2枚使うため、1投稿は4問になった（カルーセル上限10枚）

## v1.7.3

- .env.example のダミーURL（example.com）を空欄にした。そのまま使われて接続エラーになるため
- 診断レポートで、公開URLがダミーや余計なパス付きのときに警告するようにした

## v1.7.2

- 公開の確認に失敗しても、URLを必ず表示するようにした（確認失敗と公開失敗は別）
- 確認は3回まで再試行する

## v1.7.1

- 画像の書き出しで「新たに/すでにある/元が無い」を分けて表示するようにした（0件の意味が分かるように）

## v1.7.0

- 「12_画像を公開する.bat」を追加。コマンドを打たずにCloudflare Pagesへ公開できる
- Node.js が無い場合は入手先を案内し、プロジェクト名も初回だけ聞くようにした

## v1.6.2

- Metaアプリの「データ削除」「アンインストール」コールバック欄に入れるページを自動生成するようにした

## v1.6.1

- 認可画面のエラーを読み取り、登録すべきリダイレクトURIをそのまま表示するようにした
- 1349168 / 1349245 / error_code=1 に、それぞれ対処手順を出す

## v1.6.0

- Instagram の認可で、存在しないスコープを要求していたのを修正（認可画面がエラーになる原因）
- Instagram の client_id / secret を Instagram App ID / Secret に修正（Meta App ID とは別物）
- Threads の認可URLを公式表記（threads.com）に更新
- 診断レポートに client_id を表示し、IDの取り違えが分かるようにした

## v1.5.4

- スマホ用ページが公開されているかを確認し、未デプロイならデプロイ手順を出すようにした
- Cloudflare Pages が存在しないパスにもHTMLを200で返すため、中身まで見て判定する

## v1.5.3

- リリースすると配布元（main）まで自動で反映されるようにした

## v1.5.2

- 未接続の理由（トークンファイルが無い／中身が不正）を診断レポートに表示するようにした

## v1.5.1

- Instagram の認証で HTTPS のリダイレクトURIを使えるようにした（手動モードを追加）
- 診断レポートにバージョンとリダイレクトURIを表示し、エラー1349168の原因を特定しやすくした
- .env.example の Instagram のリダイレクトURI例を HTTPS に修正

## v1.5.0

- スマホから Threads と Instagram へワンタップ投稿できるようにした（Cloudflare Worker経由）
- トークンはCloudflareのSecretに置き、スマホからは合言葉だけを送る
- スマホから投稿した分をPCが取り込み、予約投稿との二重投稿を防ぐ

## v1.4.1

- 生成物（pages_media）が誤ってGitに入っていたのを取り除いた。配布物には含まれていない
- 生成物がGitに入った状態ではリリースできないよう検査を追加

## v1.4.0

- スマホ用の投稿ページを追加。PCで作ったものをケーブルなしでスマホから投稿できる
- キャプションのコピーと、画像10枚・Reel動画のまとめ保存に対応
- 推測されにくいURLに置き、検索避けを入れた

## v1.3.0

- 最終ページのCTAを4アクション（プロフィール／いいね／フォロー／共有）の結果割り当てに変更
- 結果の強さを重み付けし、共有・フォローに良い結果、プロフィールに弱い結果が出やすいようにした（毎回同じ序列にはしない）
- 重みと文言は data/cta_actions.json で変更できる
- Reelの冒頭と末尾に、認知と操作のための余白を追加

## v1.2.0

- Instagram Reel（縦動画）の書き出しに対応。予約時刻に動画を作り、音源を付けて手動で投稿する流れにした
- Reelを投稿済みとして記録すると、反応データも集まるようにした

## v1.1.2

- .env.example が更新で配布されない問題を修正
- 配布漏れをリリース時に検査するようにした

## v1.1.1

- 更新中に同名フォルダがあると更新漏れが起きる問題を修正

## v1.1.0

- ソフトの自動更新機能を追加（「更新を確認」ボタン）
- 不具合の切り分け用に、秘密情報を含まない診断レポートを追加
- 配信待ちが減ったときの自動補充を追加
- 予約済みの投稿時刻が2回目の実行で上書きされる問題を修正

## v1.0.0

- 心理テスト画像の自動生成（1投稿 = 5問 = 10枚）
- Threads / Instagram への予約投稿、TikTok への下書き転送
- 実験ID単位の記録と、1h / 6h / 24h / 72h の反応データ取得
