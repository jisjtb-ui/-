__pycache__/
*.pyc
.venv/
venv/

# 運用フォルダ（生成物はコミットしない）
output/
ready/
posted/

# 生成履歴はローカル運用データ
history.json

# 自動投稿システム
.env
*.env.local
autopost.db
autopost.db-journal
.tokens/
.autopost_cache/
autopost.log

# 実験データベース
experiments.db
experiments.db-wal
experiments.db-shm

# 診断レポート（秘密情報は含まないが共有用なのでcommitしない）
診断結果.txt
diagnose*.txt
